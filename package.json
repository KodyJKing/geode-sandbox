{
    "name": "geode-sandbox",
    "version": "0.0.0",
    "repository": "",
    "author": "Kody King <kody.j.king@gmail.com>",
    "license": "none",
    "scripts": {
        "dev": "yarn install && yarn build && yarn link geode",
        "clean": "rm -f yarn.lock && rm -rf lib && rm -rf node_modules",
        "watch": "parcel www/index.html"
    },
    "private": true,
    "devDependencies": {
        "parcel": "^2.0.0-beta.2"
    },
    "dependencies": {
    }
}